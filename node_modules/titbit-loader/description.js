'use strict'

let methods = {
  get: 'GET',
  post: 'POST',
  put: 'PUT',
  delete: 'DELETE',
  options: 'OPTIONS',
  patch: 'PATCH',
  head: 'HEAD',
  trace: 'TRACE',
  list: 'GET'
}
/**
 * 传递参数为控制器实例，然后识别出doc属性，并生成对应的文档。
 * 
 * doc属性包括对应的请求方法同名的属性。
 *
 * query属性是URL参数，param是路由参数，body是请求体参数。
 *
 * param参数格式：
 * {
 *   ':name': {
 *     values: '',
 *     default: ''
 *   }
 * }
 *
 *
 * query和body参数格式：
 * {
 *   a: {
 *    must: true,
 *    values: {},
 *    default: ''
 *   }
 * }
 *
 *
 * returns 是返回值：
 * {
 *   success: '',
 *   fail: '可以是字符串，也可以是一个对象，key值为http状态码。'
 * }
 *
 * @param ctl {object}
 *
 * */
module.exports = function (ctl) {
  
  if (typeof ctl !== 'object' || ctl.doc === undefined || typeof ctl.doc !== 'object') {
    return ''
  }

  try {
    let text = ''

    for (let k in ctl.doc) {
      if (methods[k] === undefined) continue
    

      if (['P', 'D'].indexOf(k[0]) >= 0) {

      }

    }
  } catch (err) {
    console.error(err)
    return ''
  }

}

