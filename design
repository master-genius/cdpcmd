cdpc systemd 服务依赖于nodejs

cdpc systemd服务运行后，要在/tmp目录创建cdpc_systemd_info 目录，其中包括以下内容：
    systemd_name
    webserver_info

webserver 实现功能：
    提供获取负载信息的接口
    主动汇报
    提交任务用于编排部署
    提交代码动态执行
    更新验证密钥
    编辑配置文件

cdpc命令功能：
    start APP
    stop  APP
    status APP
    restart [APP | all]
    reload
    webserver-start
    webserver-stop
    webserver-restart
    show-config
    set-config FILENAME
    save-config PATH
    systemd-start
    systemd-restart

问题：
    · 基于Web的验证很难合并到Linux本地用户
    · Linux本地root和普通用户同时兼容难以实现
    · Web服务运行在root用户，需要token验证才可以通过
    · token只有root用户才可以读取
    
需求：
    · Linux系统本地操作不仅可以使用root用户还可以使用普通用户
    · 普通用户的配置在$HOME/.cdpc/
    · 普通用户只能以自己的身份运行程序 

解决方案：
    · 对于单机多用户，采用授权的方式允许某些用户可以运行他们的程序。
    · 授权的方式为root身份运行cdpc启动授权服务。
    · 一个专门管理授权的进程，通过配置文件的方式管理用于授权的服务。
    · 对于普通用户来说，只需要把子进程配置文件放在指定目录，即可实现服务的自动化管理。
    · 对于root用户管理的配置文件都在/usr/local/cdpc/config中。
    · 专门管理用户授权的进程以root身份运行，在运行不同用户提交的配置程序，则指定对应用户的身份。
