'use strict'

const http = require('http')

http.get('http://localhost:3456/', res => {
  res.on('data', chunk => {
    console.log(chunk.toString('utf8'))
  })

  res.on('end', () => {
    console.log(' -- end --')
  })

}).on('error', err=>{
  console.error(err)
})
