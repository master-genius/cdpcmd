.TH cdpc cdpcd "mno" "child process management" "Node.js"

.SH 名称
cdpc

.SH 描述（DESCRIPTION）
cdpc是基于Node.js的进程管理服务，通过一套授权方式，让普通用户
具备运行cdpc管理所属用户进程的功能。cdpc使用一些子命令来实现各种操作。
不同的用户权限，操作的方式和可以运行的子命令有所区别，一些高级操作只有root用户才可以运行，
所以使用sudo以root身份运行可以管理所有进程，而普通用户直接使用cdpc则默认是管理用户自身的进程。

.SH 使用：

cdpc [COMMAND] [OPTIONS] [ARGUMENTS]

.SH 示例

cdpc start --all

cdpc stop --all

cdpc pause app1

.SH 授权

cdpc需要在root用户权限下管理服务，但是实际需求是每个普通用户有时候
也需要能够运行cdpc命令管理自己的服务，这是通过授权的方式实现的。
最高级管理员用户给其他普通用户授权后，普通用户就可以使用cdpc管理自己的服务。

.sp
授权命令示例：

sudo cdpc auth add xx

sudo cdpc auth remove xx

.SH 环境变量

用户cdpcd以整体替换的方式设置环境变量（不继承root的环境），分为两层。

.sp
基线层由root侧构建，只读root可信来源，且在配置加载时现算：
HOME/USER/LOGNAME/SHELL取自/etc/passwd；
PATH为 ~/.local/bin:~/bin: 加上/etc/environment中的PATH；
LANG与LC_*取自/etc/default/locale、/etc/locale.conf或/etc/sysconfig/i18n，
经locale -a校验，取不到时回落C.UTF-8。
管理员修改上述系统文件后执行cdpc reload即生效，无需重跑cdpc auth。
从旧版本升级时，已授权用户需重跑一次sudo cdpc auth add <用户>。

.sp
用户层为主目录下的 .cdpcd_env 文件，存在则由用户自己的cdpcd在启动时加载，
root不读该文件。它会被该用户全部被托管服务继承，修改后重启自身daemon即生效，
加载结果记录在 ~/.cdpc/logs/cdpcd.log 的USER-ENV条目中。

.sp
文件为.env风格，支持export前缀，支持$VAR与${VAR}展开；单引号内为纯字面量、不展开；
双引号内可用反斜杠转义美元符号、双引号、反斜杠与反引号。
不支持命令替换：值中出现$( )或反引号会跳过该行并告警。
单行出错只跳过该行，不会中止整个文件。
HOME、USER、LOGNAME、NODE_OPTIONS不允许覆盖；PATH会自动丢弃空分量与重复分量
（空分量在POSIX下等价于当前工作目录）。
文件上限64KB、512个键，建议chmod 600。

.sp
示例：

export PATH="$PATH:/opt/mytool/bin"

JAVA_HOME="${HOME}/sdk/jdk21"

.SH 子命令

对于restart、start、stop、pause、resume、remove操作，都是以下操作方式：

cdpc [控制选项] [应用名称]

--all表示所有应用。

.B restart 重启应用

.B start 启动应用

.B stop 停止应用

.B pause 暂停应用

不退出，但是进程暂时不再被系统调度。

.B resume 恢复暂停的应用。

.B remove 移除应用。

.B auth 授权用户，不加参数表示查看授权信息。

对于普通用户来说，auth仅仅是查看自己是否被授权。已被授权的用户可以使用config
添加配置文件，既可以让自己的服务程序在后台执行。对于超级用户可以通过add添加授权用户，通过remove移除已授权用户。

.B config 管理配置

config支持子命令：

.sp
config add FILE 添加配置文件。配置文件名（去掉扩展名后）即服务名，
必须以字母数字下划线开头、仅含字母数字下划线减号及 @、长度不超过50，否则会被拒绝。

.sp
config remove NAME 移除配置文件。

.sp
config show 显示所有配置文件内容。

.sp
config list 列出配置文件名。

.sp
config errors 显示最近一次配置加载报告（哪些加载成功、哪些被跳过及原因）。

.sp
config add 在复制进配置目录之前会先做语法检查（.js/.cjs/.mjs 走 node --check，
.json 走 JSON.parse），语法有错当场报错并中止，不会把坏文件留在配置目录里。
复制成功后会等待并回显 cdpcd 的配置加载报告，配置被拒当场可见；
load 与 reload 同样会回显这份报告。

.B status 显示应用状态

每个用户都仅仅能看到自己当前管理的应用。

.sp
status -l 展开详情（--runtime 交互界面里按 l 切换）。展开时表格会自动加宽到
终端宽度的 85%，且只延长最后一列——前几列宽度不变，多用户的表之间依旧对齐；
收起详情即恢复默认宽度。终端比默认宽度还窄、或输出被重定向到文件/管道时
不做加宽，宽度保持可预测。

.sp
详情里的命令过长时折成两行，第二行缩进到命令起始列对齐；优先在空格处断行，
整段没有空格（长路径）则按宽度硬断。两行仍装不下就截断第二行——
完整命令用 cdpc inspect 查看。

.sp
进程树里的命令由 cdpcd 采集时会被截断到 72 字符（cdpc 库写死，无开关），
CLI 展示前会就地补读 /proc/<pid>/cmdline 还原完整命令；进程已退出、
或读到的与采集时对不上（pid 被复用）则沿用截断值。

.B inspect 查看服务的完整运行时配置

cdpc inspect [用户:]应用名称。展示该服务真实生效的运行时配置
（命令、参数、重启策略、cgroup、资源限制等），比 config show 的磁盘原始文件更可信。

.B applog 查看应用输出日志

cdpc applog [用户:]应用名称 [-f]，等价于 cdpc log [用户:]应用名称。

.sp
应用日志（logs/apps/应用名.log）除了服务的 stdout/stderr，还记录 cdpcd 侧的
生命周期事件：以 == 开头的启动与退出（含退出码 code 与信号 signal），
以 !! 开头的失败（spawn 失败的 errno 与原因、配置被拒绝、限额未生效）。
退出行带 cause 字段时说明退出是 cdpcd 主动触发的，例如
cause=maxrss|stop|超过最大内存限制退出——没有这个字段就看不出它和被人
kill 掉有什么区别。
命令不存在这类错误只会触发 spawn 的 error 事件、不会有任何 stdout 输出，
这些 !! 行往往是现场唯一的线索。

.sp
当日志文件不存在、或文件里只有事件行而没有任何服务输出时，会自动附带一段
诊断：服务是否真的被 cdpcd 加载、配置加载报告里有没有提到它、cdpcd.log
中有哪些相关记录。-f 跟踪模式会先打印诊断再进入跟踪。

.sp
log --clear 清理应用日志与其他日志，但保留 cdpcd.log、config-errors.log
与 audit.log；log --clear-all 额外清空 cdpcd.log。

.B audit 查看命令审计日志

记录所有变更类命令（start/stop/restart/config/auth 等）的执行历史。

.B reload 与 load 的区别

.sp
load 把配置目录加载进来，新增的服务会被启动。

.sp
reload 重新加载配置，是纯粹的配置同步操作：

.sp
· 已存在且未变更的服务保持原样，不会被重启。

.sp
· 配置中已删除（或改名）的服务会被停止移除。

.sp
· 改了 command/args 的服务会重启以应用新命令。

.sp
注意：仅修改 restart、limit、env 等字段后 reload 不会重启服务，
需要显式 cdpc restart 应用名称 才能让这些改动生效。

.SH 控制通道

.sp
cdpc 命令与 cdpcd 服务之间通过 unix socket 通信，每个 daemon 一个 socket，
权限为 0600、属主专用：

.sp
· root 的 socket：/run/cdpcd/cdpcd.sock（不可用时回退到 /usr/local/cdpc/run/cdpcd.sock）

.sp
· 普通用户的 socket：$HOME/.cdpc/cdpcd.sock

.sp
普通用户只能查看和控制自己的服务，指定他人的服务（用户:应用名 形式）会被拒绝；
root 可以查看和控制所有被授权用户的服务，跨用户查询时按 uauth 记录逐个连接后汇总展示。

.sp
可以用 nc 直接调试：

.sp
echo '{"v":1,"id":1,"op":"status"}' | nc -U /run/cdpcd/cdpcd.sock

.SH 故障排查

.sp
命令报"daemon 未运行"，说明 socket 不存在或连接被拒绝，
可用 cdpc service-start 启动服务，或用 systemctl status cdpcd 查看。

.sp
命令报"cdpcd 无响应"，说明 socket 在但连接超时，daemon 可能卡死，
此时不会展示任何陈旧数据，需要检查 cdpcd 进程与 logs/cdpcd.log。

.sp
命令报"无权限连接"，说明 socket 的权限或属主与当前身份不符，
检查该路径的属主是否为对应用户、权限是否为 0600。

.sp
status 中某个用户显示为括号标注（而不是服务表格），
说明该用户的 cdpcd 未运行或不可达，其他用户的展示不受影响。

.SH 负载口径

.sp
status 的 CPU 与 MEM 两列永远是服务自身进程的占用，TREE 列是
"自身 + 全部后代进程"的合计以及树内进程数，单进程服务显示 -。

.sp
之所以要分开：Linux 只把"已被回收的子进程"的 CPU 时间累计到父进程，
活着的子进程不计入，内存也只算自己。所以包装脚本、master/worker、
cluster 主进程这类服务，自身看起来几乎不消耗资源，必须看 TREE 列。

.sp
cdpc status -l 应用名 （或 --runtime 模式下按 l 键）会展开进程树，
逐个列出树内进程的 pid、自身 CPU、自身内存与命令，用于定位具体是哪个进程在耗。

.sp
cdpc inspect 应用名 同样会输出"进程树"段落。

.sp
注意：资源限制 limit.maxrss 的判定仍按服务自身内存，没有按进程树判定，
避免包装型服务因子进程内存波动被误杀。配置了 cgroup 的服务由内核按整组限制。

.sp
单位不要混：limit.maxrss / limit.rssOffset 的单位是 KB，由 cdpc 自己轮询判定
后重启或停止；cgroup 的 memory 单位是字节，由内核强制执行，超限直接 OOM kill。
limit.maxRestart 是次数，超过后转为停止不再重启。
limit 里的 maxtime / frequency / maxdaylimit 在当前版本未实现，写了不生效，
cdpc inspect 会把它们标注出来。

.sp
cgroup 的 cpu 有两种写法：数字按百分比，数组是 [quota, period] 微秒。
内核硬约束：quota >= 1000，period 在 1000~1000000 之间。
cpu 写 0 或不写都表示不限制，与 memory、pids 的 0 语义一致。

.sp
注意 cpu.max 的 quota/period 表示的是"多少个 CPU"，跟机器核数无关
（systemd 手册：relative to the total CPU time available on one CPU）。
所以 cpu: 50 是"半个 CPU"而不是"半台机器"。cdpcd 自带的 9 个预设组
（cdpcd-25-limit 等）已经按核数缩放，组名里的百分比是占全机的意思；
单核机器上与旧版取值一致。cpuset（cpus 选项）才是"能用哪些核"。
配了 cgroup 但实际没能入组时（非 root、组未创建等）会有
--WARN-CGROUP-ATTACH-- 告警，并说明该服务当前没有资源限制。

.SH cluster 模式

.sp
nodejs 服务在配置里加 cluster: true 与 workers: N，即可启动 N 个 worker
共享同一端口，应用不需要改代码。workers 写 0 表示按 CPU 核数，上限 64。

.sp
cdpc 自身不做 cluster 管理：它只多管一个 launcher 进程，N 个 worker 的
fork、补员与收尾都在 launcher 内完成。所以在 status 里它仍然是一个服务，
TREE 列显示 launcher 与 worker 的合计，status -l 可展开到每个 worker。

.sp
worker 异常终止会被自动补员；worker 起来就崩时会触发熔断，
launcher 退出并交由 cdpc 按 restart 策略整组重启。

.sp
停机是分层的：worker 先优雅收尾，launcher 3 秒后对未退出的 worker 强制终止，
cdpc 5 秒兜底。worker 会收到两次 SIGTERM（cdpc 与 launcher 各一次），
所以应用的 SIGTERM 处理需要幂等。

.sp
pause 与 stop、remove 一样作用于整个服务的进程树，包括更深层的孙子进程：
cluster 服务被 pause 时 launcher 与全部 worker 都会进入停止状态；
用 shell 脚本拉起一组进程的服务同样是整棵树一起停，
不会出现"看着暂停了但还在占 CPU"的情况。

.sp
在暂停状态下执行 stop，cdpc 会先恢复整棵树再发终止信号，优雅收尾不会丢；
执行 remove 则直接强制终止整棵树。重复 pause 或 resume 都没有副作用。

.sp
注意：资源限制 limit.maxrss 对 cluster 服务无效，它只测 launcher 自身的内存。
cluster 服务的内存限制请使用 cgroup（内核按整个组限制，memory 单位为字节）。

.sp
cluster 目前只支持 nodejs 服务；非 node 程序、node -e 这类没有入口文件的形式
会被拒绝加载，原因写入配置加载报告（cdpc config errors 可查看）。
